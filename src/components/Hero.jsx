function Hero() {
  return (
    <section className="hero">
      <h1 className="hero-heading">Showcase Your Projects</h1>
      <p className="hero-tagline">
        Build, track, and share everything you've created — all in one place.
      </p>
    </section>
  );
}

export default Hero;
